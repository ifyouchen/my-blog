<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myblog.growth.infrastructure.repository.persistence.mapper.AdminPointAdjustLogMapper">

    <resultMap id="BaseResultMap" type="com.myblog.growth.infrastructure.repository.persistence.entity.AdminPointAdjustLogDO">
        <id     column="id"                 property="id"/>
        <result column="target_user_id"     property="targetUserId"/>
        <result column="delta"              property="delta"/>
        <result column="reason"             property="reason"/>
        <result column="biz_no"             property="bizNo"/>
        <result column="operator_user_id"   property="operatorUserId"/>
        <result column="operator_username"  property="operatorUsername"/>
        <result column="created_at"         property="createdAt"/>
        <result column="deleted_at"         property="deletedAt"/>
        <result column="version"            property="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, target_user_id, delta, reason, biz_no,
        operator_user_id, operator_username, created_at, deleted_at, version
    </sql>

    <!-- 插入管理员调分日志，回填自增主键 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_point_adjust_log
            (target_user_id, delta, reason, biz_no, operator_user_id, operator_username, created_at, version)
        VALUES
            (#{targetUserId}, #{delta}, #{reason}, #{bizNo},
             #{operatorUserId}, #{operatorUsername}, #{createdAt}, #{version})
    </insert>

</mapper>

