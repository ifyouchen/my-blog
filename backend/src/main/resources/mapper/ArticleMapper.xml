<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myblog.infrastructure.repository.persistence.mapper.ArticleMapper">

    <resultMap id="BaseResultMap" type="com.myblog.infrastructure.repository.persistence.entity.ArticleDO">
        <id column="id" property="id"/>
        <result column="author_id" property="authorId"/>
        <result column="title" property="title"/>
        <result column="summary" property="summary"/>
        <result column="content" property="content"/>
        <result column="cover_url" property="coverUrl"/>
        <result column="category" property="category"/>
        <result column="offline_reason" property="offlineReason"/>
        <result column="status" property="status"/>
        <result column="view_count" property="viewCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="published_at" property="publishedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="version" property="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, author_id, title, summary, content, cover_url, category, offline_reason, status,
        view_count, like_count, favorite_count, comment_count, published_at,
        created_at, updated_at, deleted_at, version
    </sql>

    <sql id="List_Column_List">
        id, author_id, title, summary, cover_url, category, offline_reason, status,
        view_count, like_count, favorite_count, comment_count, published_at,
        created_at, updated_at, deleted_at, version
    </sql>

    <sql id="List_Alias_Column_List">
        a.id, a.author_id, a.title, a.summary, a.cover_url, a.category, a.offline_reason, a.status,
        a.view_count, a.like_count, a.favorite_count, a.comment_count, a.published_at,
        a.created_at, a.updated_at, a.deleted_at, a.version
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM blog_article
        WHERE id = #{id}
        AND deleted_at IS NULL
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <select id="selectAdminPage" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE deleted_at IS NULL
        AND status != 'DELETED'
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAdminPage" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE deleted_at IS NULL
        AND status != 'DELETED'
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE id IN
        <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
        AND deleted_at IS NULL
    </select>

    <select id="selectPublished" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE status = 'PUBLISHED'
        AND deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                title LIKE CONCAT('%', #{keyword}, '%')
                OR summary LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
        <choose>
            <when test="sort == 'hot'">
                ORDER BY view_count DESC, published_at DESC, id DESC
            </when>
            <when test="sort == 'featured'">
                ORDER BY like_count DESC, published_at DESC, id DESC
            </when>
            <otherwise>
                ORDER BY published_at DESC, id DESC
            </otherwise>
        </choose>
        <if test="limit != null">
            LIMIT #{limit}
        </if>
        <if test="offset != null">
            OFFSET #{offset}
        </if>
    </select>

    <select id="selectByAuthorId" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        ORDER BY updated_at DESC, id DESC
    </select>

    <select id="selectByAuthorIdWithStatus" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByAuthorIdWithStatus" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>

    <select id="selectPublishedByAuthorId" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND status = 'PUBLISHED'
        AND deleted_at IS NULL
        ORDER BY published_at DESC, id DESC
    </select>

    <select id="selectLatestByAuthorId" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        ORDER BY updated_at DESC, id DESC
        LIMIT 1
    </select>

    <select id="selectAuthorMetrics" resultMap="AuthorArticleMetricsResultMap">
        SELECT
            COUNT(*) AS article_count,
            COALESCE(SUM(view_count), 0) AS total_views,
            COALESCE(SUM(like_count), 0) AS total_likes,
            COALESCE(SUM(favorite_count), 0) AS total_favorites,
            COALESCE(SUM(comment_count), 0) AS total_comments,
            SUM(CASE WHEN status = 'DRAFT' THEN 1 ELSE 0 END) AS draft_count,
            SUM(CASE WHEN status = 'PUBLISHED' THEN 1 ELSE 0 END) AS published_count,
            SUM(CASE WHEN status = 'OFFLINE' THEN 1 ELSE 0 END) AS offline_count,
            SUM(CASE WHEN status = 'DELETED' THEN 1 ELSE 0 END) AS deleted_count
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>

    <select id="selectHotPublishedByAuthorId" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND status = 'PUBLISHED'
        AND deleted_at IS NULL
        ORDER BY view_count DESC, like_count DESC, published_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="selectAuthorPerformance" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id = #{authorId}
        AND deleted_at IS NULL
        AND status != 'DELETED'
        <choose>
            <when test="sort == 'like'">
                ORDER BY like_count DESC, view_count DESC, updated_at DESC, id DESC
            </when>
            <when test="sort == 'favorite'">
                ORDER BY favorite_count DESC, view_count DESC, updated_at DESC, id DESC
            </when>
            <when test="sort == 'comment'">
                ORDER BY comment_count DESC, view_count DESC, updated_at DESC, id DESC
            </when>
            <when test="sort == 'updated'">
                ORDER BY updated_at DESC, id DESC
            </when>
            <otherwise>
                ORDER BY view_count DESC, like_count DESC, updated_at DESC, id DESC
            </otherwise>
        </choose>
        LIMIT #{limit}
    </select>

    <select id="selectAuthorTrendPoints" resultType="com.myblog.infrastructure.repository.persistence.entity.DashboardTrendPointDO">
        SELECT
            stat_date AS statDate,
            COALESCE(SUM(view_count), 0) AS viewCount,
            COALESCE(SUM(like_count), 0) AS likeCount,
            COALESCE(SUM(favorite_count), 0) AS favoriteCount,
            COALESCE(SUM(comment_count), 0) AS commentCount
        FROM (
            SELECT DATE(v.created_at) AS stat_date,
                   COUNT(1) AS view_count,
                   0 AS like_count,
                   0 AS favorite_count,
                   0 AS comment_count
            FROM blog_article_view v
            INNER JOIN blog_article a ON a.id = v.article_id
            WHERE a.author_id = #{authorId}
            AND a.deleted_at IS NULL
            AND v.deleted_at IS NULL
            AND v.created_at &gt;= #{startDate}
            AND v.created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            GROUP BY DATE(v.created_at)

            UNION ALL

            SELECT DATE(l.created_at) AS stat_date,
                   0 AS view_count,
                   COUNT(1) AS like_count,
                   0 AS favorite_count,
                   0 AS comment_count
            FROM blog_article_like l
            INNER JOIN blog_article a ON a.id = l.article_id
            WHERE a.author_id = #{authorId}
            AND a.deleted_at IS NULL
            AND l.deleted_at IS NULL
            AND l.created_at &gt;= #{startDate}
            AND l.created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            GROUP BY DATE(l.created_at)

            UNION ALL

            SELECT DATE(f.created_at) AS stat_date,
                   0 AS view_count,
                   0 AS like_count,
                   COUNT(1) AS favorite_count,
                   0 AS comment_count
            FROM blog_article_favorite f
            INNER JOIN blog_article a ON a.id = f.article_id
            WHERE a.author_id = #{authorId}
            AND a.deleted_at IS NULL
            AND f.deleted_at IS NULL
            AND f.created_at &gt;= #{startDate}
            AND f.created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            GROUP BY DATE(f.created_at)

            UNION ALL

            SELECT DATE(c.created_at) AS stat_date,
                   0 AS view_count,
                   0 AS like_count,
                   0 AS favorite_count,
                   COUNT(1) AS comment_count
            FROM blog_comment c
            INNER JOIN blog_article a ON a.id = c.article_id
            WHERE a.author_id = #{authorId}
            AND a.deleted_at IS NULL
            AND c.deleted_at IS NULL
            AND c.status = 'PUBLISHED'
            AND c.created_at &gt;= #{startDate}
            AND c.created_at &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
            GROUP BY DATE(c.created_at)
        ) trend_rows
        GROUP BY stat_date
        ORDER BY stat_date ASC
    </select>

    <select id="selectPublishedByAuthorIds" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM blog_article
        WHERE author_id IN
        <foreach collection="authorIds" item="authorId" open="(" separator="," close=")">
            #{authorId}
        </foreach>
        AND status = 'PUBLISHED'
        AND deleted_at IS NULL
        <choose>
            <when test="sort == 'featured'">
                ORDER BY like_count DESC, published_at DESC, id DESC
            </when>
            <when test="sort == 'latest'">
                ORDER BY published_at DESC, id DESC
            </when>
            <otherwise>
                ORDER BY view_count DESC, published_at DESC, id DESC
            </otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPublishedByAuthorIds" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE author_id IN
        <foreach collection="authorIds" item="authorId" open="(" separator="," close=")">
            #{authorId}
        </foreach>
        AND status = 'PUBLISHED'
        AND deleted_at IS NULL
    </select>

    <select id="countById" resultType="int">
        SELECT COUNT(1)
        FROM blog_article
        WHERE id = #{id}
    </select>

    <select id="selectNextId" resultType="long">
        SELECT COALESCE(MAX(id), 100) + 1
        FROM blog_article
        WHERE id &lt; 10000000000
    </select>

    <insert id="insert" parameterType="com.myblog.infrastructure.repository.persistence.entity.ArticleDO">
        INSERT INTO blog_article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id,
            author_id,
            title,
            summary,
            content,
            cover_url,
            category,
            offline_reason,
            status,
            view_count,
            like_count,
            favorite_count,
            comment_count,
            published_at,
            created_at,
            updated_at,
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{id},
            #{authorId},
            #{title},
            #{summary},
            #{content},
            #{coverUrl},
            #{category},
            #{offlineReason},
            #{status},
            #{viewCount},
            #{likeCount},
            #{favoriteCount},
            #{commentCount},
            #{publishedAt},
            #{createdAt},
            #{updatedAt},
        </trim>
    </insert>

    <update id="update" parameterType="com.myblog.infrastructure.repository.persistence.entity.ArticleDO">
        UPDATE blog_article
        SET title = #{title},
            summary = #{summary},
            content = #{content},
            cover_url = #{coverUrl},
            category = #{category},
            offline_reason = #{offlineReason},
            status = #{status},
            view_count = #{viewCount},
            like_count = #{likeCount},
            favorite_count = #{favoriteCount},
            comment_count = #{commentCount},
            published_at = #{publishedAt},
            updated_at = #{updatedAt},
            version = version + 1
        WHERE id = #{id}
        AND version = #{version}
        AND deleted_at IS NULL
    </update>

    <!-- INSERT ... ON DUPLICATE KEY UPDATE，省去 countById 前置查询 -->
    <insert id="insertOrUpdate" parameterType="com.myblog.infrastructure.repository.persistence.entity.ArticleDO">
        INSERT INTO blog_article (
            id, author_id, title, summary, content, cover_url, category, offline_reason, status,
            view_count, like_count, favorite_count, comment_count, published_at,
            created_at, updated_at
        ) VALUES (
            #{id}, #{authorId}, #{title}, #{summary}, #{content}, #{coverUrl}, #{category}, #{offlineReason}, #{status},
            #{viewCount}, #{likeCount}, #{favoriteCount}, #{commentCount}, #{publishedAt},
            #{createdAt}, #{updatedAt}
        )
        ON DUPLICATE KEY UPDATE
            title = #{title},
            summary = #{summary},
            content = #{content},
            cover_url = #{coverUrl},
            category = #{category},
            offline_reason = #{offlineReason},
            status = #{status},
            view_count = #{viewCount},
            like_count = #{likeCount},
            favorite_count = #{favoriteCount},
            comment_count = #{commentCount},
            published_at = #{publishedAt},
            updated_at = NOW(),
            version = version + 1
    </insert>

    <select id="selectTagNamesByArticleId" resultType="string">
        SELECT tag_name
        FROM blog_article_tag
        WHERE article_id = #{articleId}
        AND deleted_at IS NULL
        ORDER BY id ASC
    </select>

    <select id="selectTagRowsByArticleIds" resultType="com.myblog.infrastructure.repository.persistence.entity.ArticleTagRowDO">
        SELECT article_id AS articleId, tag_name AS tagName
        FROM blog_article_tag
        WHERE article_id IN
        <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
        AND deleted_at IS NULL
        ORDER BY article_id ASC, id ASC
    </select>

    <update id="logicDeleteTagsByArticleId">
        UPDATE blog_article_tag
        SET deleted_at = NOW(),
            updated_at = NOW(),
            version = version + 1
        WHERE article_id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <select id="countTag" resultType="int">
        SELECT COUNT(1)
        FROM blog_article_tag
        WHERE article_id = #{articleId}
        AND tag_name = #{tagName}
    </select>

    <update id="restoreTag">
        UPDATE blog_article_tag
        SET deleted_at = NULL,
            updated_at = NOW(),
            version = version + 1
        WHERE article_id = #{articleId}
        AND tag_name = #{tagName}
    </update>

    <insert id="insertTag">
        INSERT INTO blog_article_tag (article_id, tag_name)
        VALUES (#{articleId}, #{tagName})
    </insert>

    <select id="countPublished" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE status = 'PUBLISHED'
        AND deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                title LIKE CONCAT('%', #{keyword}, '%')
                OR summary LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
    </select>

    <select id="selectPublishedEnhanced" resultMap="BaseResultMap">
        SELECT <include refid="List_Alias_Column_List"/>
        FROM blog_article a
        LEFT JOIN blog_user u ON a.author_id = u.id
        WHERE a.status = 'PUBLISHED'
        AND a.deleted_at IS NULL
        AND u.deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                a.title LIKE CONCAT('%', #{keyword}, '%')
                OR a.summary LIKE CONCAT('%', #{keyword}, '%')
                OR a.content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND a.category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND a.id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
        <if test="authorKeyword != null and authorKeyword != ''">
            AND (
                u.username LIKE CONCAT('%', #{authorKeyword}, '%')
                OR u.nickname LIKE CONCAT('%', #{authorKeyword}, '%')
            )
        </if>
        <if test="dateFrom != null and dateFrom != ''">
            AND a.published_at &gt;= #{dateFrom}
        </if>
        <if test="dateTo != null and dateTo != ''">
            AND a.published_at &lt; DATE_ADD(#{dateTo}, INTERVAL 1 DAY)
        </if>
        <choose>
            <when test="sort == 'hot'">
                ORDER BY a.view_count DESC, a.published_at DESC, a.id DESC
            </when>
            <when test="sort == 'featured'">
                ORDER BY a.like_count DESC, a.published_at DESC, a.id DESC
            </when>
            <otherwise>
                ORDER BY a.published_at DESC, a.id DESC
            </otherwise>
        </choose>
        <if test="limit != null">
            LIMIT #{limit}
        </if>
        <if test="offset != null">
            OFFSET #{offset}
        </if>
    </select>

    <select id="countPublishedEnhanced" resultType="long">
        SELECT COUNT(1)
        FROM blog_article a
        LEFT JOIN blog_user u ON a.author_id = u.id
        WHERE a.status = 'PUBLISHED'
        AND a.deleted_at IS NULL
        AND u.deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                a.title LIKE CONCAT('%', #{keyword}, '%')
                OR a.summary LIKE CONCAT('%', #{keyword}, '%')
                OR a.content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND a.category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND a.id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
        <if test="authorKeyword != null and authorKeyword != ''">
            AND (
                u.username LIKE CONCAT('%', #{authorKeyword}, '%')
                OR u.nickname LIKE CONCAT('%', #{authorKeyword}, '%')
            )
        </if>
        <if test="dateFrom != null and dateFrom != ''">
            AND a.published_at &gt;= #{dateFrom}
        </if>
        <if test="dateTo != null and dateTo != ''">
            AND a.published_at &lt; DATE_ADD(#{dateTo}, INTERVAL 1 DAY)
        </if>
    </select>

    <select id="selectPublishedEnhancedByAuthorIds" resultMap="BaseResultMap">
        SELECT <include refid="List_Alias_Column_List"/>
        FROM blog_article a
        LEFT JOIN blog_user u ON a.author_id = u.id
        WHERE a.author_id IN
        <foreach collection="authorIds" item="authorId" open="(" separator="," close=")">
            #{authorId}
        </foreach>
        AND a.status = 'PUBLISHED'
        AND a.deleted_at IS NULL
        AND u.deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                a.title LIKE CONCAT('%', #{keyword}, '%')
                OR a.summary LIKE CONCAT('%', #{keyword}, '%')
                OR a.content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND a.category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND a.id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
        <if test="authorKeyword != null and authorKeyword != ''">
            AND (
                u.username LIKE CONCAT('%', #{authorKeyword}, '%')
                OR u.nickname LIKE CONCAT('%', #{authorKeyword}, '%')
            )
        </if>
        <if test="dateFrom != null and dateFrom != ''">
            AND a.published_at &gt;= #{dateFrom}
        </if>
        <if test="dateTo != null and dateTo != ''">
            AND a.published_at &lt; DATE_ADD(#{dateTo}, INTERVAL 1 DAY)
        </if>
        <choose>
            <when test="sort == 'featured'">
                ORDER BY a.like_count DESC, a.published_at DESC, a.id DESC
            </when>
            <when test="sort == 'latest'">
                ORDER BY a.published_at DESC, a.id DESC
            </when>
            <otherwise>
                ORDER BY a.view_count DESC, a.published_at DESC, a.id DESC
            </otherwise>
        </choose>
        <if test="limit != null">
            LIMIT #{limit}
        </if>
        <if test="offset != null">
            OFFSET #{offset}
        </if>
    </select>

    <select id="countPublishedEnhancedByAuthorIds" resultType="long">
        SELECT COUNT(1)
        FROM blog_article a
        LEFT JOIN blog_user u ON a.author_id = u.id
        WHERE a.author_id IN
        <foreach collection="authorIds" item="authorId" open="(" separator="," close=")">
            #{authorId}
        </foreach>
        AND a.status = 'PUBLISHED'
        AND a.deleted_at IS NULL
        AND u.deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (
                a.title LIKE CONCAT('%', #{keyword}, '%')
                OR a.summary LIKE CONCAT('%', #{keyword}, '%')
                OR a.content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="category != null and category != ''">
            AND a.category = #{category}
        </if>
        <if test="tag != null and tag != ''">
            AND a.id IN (
                SELECT article_id
                FROM blog_article_tag
                WHERE tag_name = #{tag}
                AND deleted_at IS NULL
            )
        </if>
        <if test="authorKeyword != null and authorKeyword != ''">
            AND (
                u.username LIKE CONCAT('%', #{authorKeyword}, '%')
                OR u.nickname LIKE CONCAT('%', #{authorKeyword}, '%')
            )
        </if>
        <if test="dateFrom != null and dateFrom != ''">
            AND a.published_at &gt;= #{dateFrom}
        </if>
        <if test="dateTo != null and dateTo != ''">
            AND a.published_at &lt; DATE_ADD(#{dateTo}, INTERVAL 1 DAY)
        </if>
    </select>

    <select id="countVisible" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE deleted_at IS NULL
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE deleted_at IS NULL
        AND status = #{status}
    </select>

    <select id="countCreatedOn" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE deleted_at IS NULL
        AND created_at &gt;= #{date}
        AND created_at &lt; DATE_ADD(#{date}, INTERVAL 1 DAY)
    </select>

    <select id="countCreatedSince" resultType="long">
        SELECT COUNT(1)
        FROM blog_article
        WHERE deleted_at IS NULL
        AND created_at &gt;= #{date}
    </select>

    <select id="countPublishedAuthors" resultType="long">
        SELECT COUNT(DISTINCT author_id)
        FROM blog_article
        WHERE status = 'PUBLISHED'
        AND deleted_at IS NULL
    </select>

    <select id="selectAuthorArticleStats" resultMap="AuthorArticleStatsResultMap">
        SELECT
            author_id,
            COUNT(*) as article_count,
            SUM(view_count) as total_views,
            SUM(like_count) as total_likes
        FROM blog_article
        WHERE status = 'PUBLISHED'
        AND deleted_at IS NULL
        GROUP BY author_id
        ORDER BY total_views DESC, total_likes DESC
        LIMIT #{limit}
    </select>

    <resultMap id="AuthorArticleStatsResultMap" type="com.myblog.infrastructure.repository.persistence.entity.AuthorArticleStatsDO">
        <id column="author_id" property="authorId"/>
        <result column="article_count" property="articleCount"/>
        <result column="total_views" property="totalViews"/>
        <result column="total_likes" property="totalLikes"/>
    </resultMap>

    <resultMap id="AuthorArticleMetricsResultMap" type="com.myblog.infrastructure.repository.persistence.entity.AuthorArticleMetricsDO">
        <result column="article_count" property="articleCount"/>
        <result column="total_views" property="totalViews"/>
        <result column="total_likes" property="totalLikes"/>
        <result column="total_favorites" property="totalFavorites"/>
        <result column="total_comments" property="totalComments"/>
        <result column="draft_count" property="draftCount"/>
        <result column="published_count" property="publishedCount"/>
        <result column="offline_count" property="offlineCount"/>
        <result column="deleted_count" property="deletedCount"/>
    </resultMap>

    <!-- 原子递增点赞数，避免并发 Read-Modify-Write 丢失更新 -->
    <update id="incrementLikeCount">
        UPDATE blog_article
        SET like_count = like_count + 1,
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递减点赞数，防止降为负数 -->
    <update id="decrementLikeCount">
        UPDATE blog_article
        SET like_count = GREATEST(like_count - 1, 0),
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递增收藏数 -->
    <update id="incrementFavoriteCount">
        UPDATE blog_article
        SET favorite_count = favorite_count + 1,
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递减收藏数，防止降为负数 -->
    <update id="decrementFavoriteCount">
        UPDATE blog_article
        SET favorite_count = GREATEST(favorite_count - 1, 0),
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递增评论数 -->
    <update id="incrementCommentCount">
        UPDATE blog_article
        SET comment_count = comment_count + 1,
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递减评论数，支持批量（删除楼层时同时减去回复数） -->
    <update id="decrementCommentCount">
        UPDATE blog_article
        SET comment_count = GREATEST(comment_count - #{decrement}, 0),
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>

    <!-- 原子递增阅读数 -->
    <update id="incrementViewCount">
        UPDATE blog_article
        SET view_count = view_count + 1,
            updated_at = NOW()
        WHERE id = #{articleId}
        AND deleted_at IS NULL
    </update>
</mapper>
