Task-1：首页双通道（推荐/关注）
输入给 AI
文件：frontend/src/views/HomeView.vue

目标：新增 feedTab，query 同步，登录态默认策略

实施步骤
新增 feedTab 状态与解析函数

首次进入根据登录态设置默认 tab

切换 tab 更新 query：feedTab

按 tab 调用不同列表 API（无接口时先 fallback）

添加 tab UI 与空态

验证命令
cd frontend && npm run build

验收
游客/登录用户默认 tab 符合预期

刷新可恢复 tab 状态

Task-2：搜索无结果智能兜底
输入给 AI
文件：frontend/src/views/SearchView.vue

目标：无结果推荐词 + 热门标签 + 统一路由切换

实施步骤
新增 suggestKeywords 计算逻辑

空态区域新增建议词与热门标签

点击建议词触发 syncRoute + search

所有 tab 切换统一走 switchSearchTab

验证命令
cd frontend && npm run build

验收
无结果时至少2种可行动作

query 同步正确

Task-3：阅读完成触发推荐
输入给 AI
文件：frontend/src/views/ArticleDetailView.vue

目标：进度>=70显示一次性推荐条

实施步骤
新增 deepReadTriggered 状态

在 handleScroll 判断触发阈值

渲染“继续阅读推荐条”

加埋点函数调用

验收
滚动过阈值后出现推荐条

仅触发一次

Task-4：Dashboard 任务中心
输入给 AI
文件：frontend/src/views/DashboardView.vue

目标：规则化任务卡片与 CTA

实施步骤
新增任务规则计算器 buildTaskCards()

渲染任务卡组件区

CTA 跳转对应页面

空态并存校验

验收
典型三场景任务都可出现

CTA 可达

Task-5：埋点最小闭环
输入给 AI
文件：frontend/src/utils/track.js（新增）

目标：统一 track(event, props)，先 console + 可扩展上报

实施步骤
创建 track 工具

接入 A/B/C/D 的关键事件

用环境变量开关控制

验收
开发环境能看到事件日志

不影响现有主流程

Task-6：e2e 冒烟补齐
输入给 AI
文件：frontend/tests/e2e/smoke.spec.js

目标：新增 4 条冒烟

用例建议
首页 tab 切换 query 同步

搜索无结果建议词点击

阅读70%触发推荐

Dashboard 任务卡 CTA 跳转